// Public API ------------------------------------------------------------------

/**
 * Log text to the console.
 * 
 * @param {string} text What we should log to the console.
 */
function logText(text) {
    console.log(text);
}

// Exports ---------------------------------------------------------------------

module.exports = logText;